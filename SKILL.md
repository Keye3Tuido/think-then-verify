---
name: tv
description: 每个回复自动执行 Think → Verify → Decide，自查过程可见。
---

# Think-Then-Verify

任何用户输入必须走完 Think → Verify → Decide 流程。每阶段为独立**任务**；`=== 最终回答 ===` 之前内容均为思考过程。整个流程必须以已验证证据推进，禁止基于猜测制造分支、回退或重复推导。

## 术语定义

| 术语 | 定义 |
|------|------|
| 任务 | 独立执行单元，以 `[任务:类型 #序号]` 标题开始 |
| 最终回答标志 | `=== 最终回答 ===`，每条回复仅出现一次，必须是**纯文本独占一行**：前后无 Markdown、无引用、无列表、无代码块、无任何标签包裹；标志后**空一行**再接正文 |
| 回答正文 | 最终回答标志之后、违背声明之前的内容 |
| 违背声明 | 正文之后追加的违背规则声明块 |
| 最终输出 | 回答正文 + 违背声明（如有） |

## 歧义与猜测

任何阶段都**不得**把未经证实的判断当作前提，也**不得**基于猜测进行推导、分支、回退或输出。

- 遇到用户意图歧义，必须先澄清；不得按“可能是”“或许是”“通常是”自行补全用户意思
- 遇到项目事实不确定，必须先证实；不得按“可能存在”“大概这样”“推测项目实际如此”自行补全项目现实
- 任何推导、分支或决策，都必须建立在已经证实或明确确认的条件之上
- Think：只允许基于已知事实、明确规则和可验证依据进行推理，不得把猜测包装成结论
- Verify：只允许对已给出的候选和规则做逐条验证，不得补写猜测性的前提
- Decide：只允许依据 Verify 结果决定下一步，不得把猜测当成通过条件
- Output：只允许输出已验证内容；凡无法验证的部分，必须停下并澄清，不能直接猜写

## 项目规则文档

项目**必须**在根目录按对话维护 `.tv/〔conv〕/rules.yml`，以 YAML 格式显式记录当前对话所有需遵守的规则。该文档是 Verify 阶段的**权威规则来源**，不同对话的规则文件相互隔离。

### `〔conv〕` 推导规则

`〔conv〕` 为对话标识，**必须**按以下优先级确定，不可自行编造：

1. 若系统提供会话/对话 ID（如 VS Code `chatSessionId`），**直接使用**该 ID
2. 若系统未提供稳定 ID，**必须向用户索取一个显式的对话键**（例如短标识、会话名或编号），禁止从消息内容反推
3. 同一对话的所有轮次**必须使用同一个** `〔conv〕` 值，不得每轮重新推导

> 若当前会话没有稳定 ID 且用户也未提供显式对话键，**必须暂停并要求用户指定**。
>
> ⚠️ `.tv/〔conv〕/rules.yml` 中的 `〔conv〕` 是**对话子目录名**，不是文件名或根目录名；正确结构是 `.tv/〔conv〕/rules.yml`，而不是 `.tv/conv/rules.yml` 或 `.tv/rules.yml`。

### 路径约束

- ✅ 正确路径：`.tv/〔conv〕/rules.yml`（`〔conv〕` 为子目录名，`rules.yml` 在其下）
- ❌ **严禁**在以下位置创建 rules.yml：
  - 项目根目录（`rules.yml`）
  - `.tv/rules.yml`（`〔conv〕` 子目录缺失）
  - `.tv/〔conv〕.yml`（文件而非子目录）
  - 任何非 `.tv/〔conv〕/` 子目录的位置
- 写入前**必须**校验目标路径匹配 `.tv/〔conv〕/rules.yml` 格式（`〔conv〕` 非空、目录层级正确）

### 规则 schema

```yaml
# .tv/〔conv〕/rules.yml
rules:
  - source: system
    desc: 用中文回复
  - source: user
    desc: <用户要求>
  - source: agreement
    desc: <约定>
```

| source 值 | 对应标签 | 含义 |
|-----------|----------|------|
| `system` | `[系统]` | 系统/平台/运行环境规则 |
| `user` | `[用户]` | 用户显式要求 |
| `agreement` | `[约定]` | 对话中达成的共识/偏好 |
| `secrecy` | `[保密性规则]` | 存在但不可披露的规则 |

- 每条规则的 `desc` 必须**独立可验证**，与 TV 规则计数逻辑对齐。
- 文档纳入版本控制，随项目演进持续更新。

### 规则 vs 需求：什么写入 rules.yml

**规则**（写入 yml）与**需求**（不写入）的核心区别：

| 维度 | 规则（写入 yml） | 需求（不写入） |
|------|-----------------|---------------|
| 作用域 | 约束 agent **行为方式**，跨轮次生效 | 描述本次**要完成的任务**，仅本轮有效 |
| 例 | "用中文回复"、"保持简洁"、"代码要加注释" | "帮我写排序函数"、"地球绕什么转"、"删除 legacy 字段" |
| 判定 | 去掉本轮具体内容后，句子是否仍对 agent 有意义？ | 去掉本轮具体内容后，句子是否变得无意义？ |

**三步判定法**（三问全"是"才写入）：

1. 是否约束 agent 的**行为方式**（而非本次输出内容）？
2. 是否在**后续轮次中仍需遵守**？
3. 是否**独立于本轮具体任务**仍有意义？

> 典型判断："用表格展示"、"保持简洁"、"每次都先列时间复杂度" 属于规则；"帮我写排序函数"、"分析这段代码" 属于需求。

### ⚠️ 硬性读写要求（agent 不得跳过）

| 时机 | 动作 | 强制性 |
|------|------|--------|
| **列出/收集规则前** | **必须先 `read_file` 读取当前对话的 `.tv/〔conv〕/rules.yml` 全文** | 🔴 MUST |
| **用户提出新的行为规则/约定/偏好时** | **必须立即用 `replace_string_in_file` 或 `create_file` 更新当前对话的 `.tv/〔conv〕/rules.yml`**（仅写入规则，不写入任务需求） | 🔴 MUST |
| **规则被修改或废弃时** | **必须同步更新当前对话的 `.tv/〔conv〕/rules.yml`，删除或修改对应条目** | 🔴 MUST |
| **创建 rules.yml 前** | **必须校验路径符合 `.tv/〔conv〕/rules.yml` 格式**（`〔conv〕` 非空、目录层级正确、不在禁止路径清单中） | 🔴 MUST |

> ❌ 禁止行为：凭记忆列出规则而不读取当前对话的 `.tv/〔conv〕/rules.yml`。
> ❌ 禁止行为：用户提出新规则后仅记录在对话中而不写入 `.tv/〔conv〕/rules.yml`。
> ❌ 禁止行为：在根目录、`.tv/` 直接下级等禁止路径创建 rules.yml。
> 违反以上任一条 → Verify 直接判定为规则收集不完整，本轮 Think 候选作废。

## 适用范围

任何用户输入都必须执行完整流程——寒暄、致谢、纠正、追问、单字、表情均不例外。

## 任务驱动工作流程

"任务"指 agent 框架中的 task（task list 条目），非纯文本标记。

### Agent Task 要求

支持 task list 的 agent **必须**将每阶段作为独立 task 执行：

- 名称格式：`Think #N`、`Verify #N`、`Decide #N`、`Output`
- 首次执行时**一次性预创建全部 10 个 task**
- 进入下一轮思考时推进到已预创建的下一轮 task，不动态创建

#### 支持 task list 的 agent

| Agent / IDE | Task List 形式 |
|-------------|---------------|
| Claude Code | 内置 Task 管理系统 |
| Cursor（1.2+） | Agent Planning to-do list |
| VS Code Copilot（Agent 模式） | Plan agent + todo list |

### 降级策略：tasks.md 单文档

环境不支持 task list 时（Kiro Vibe、DeepSeek TUI、纯对话终端等），agent 创建 `.tv/〔conv〕/tasks.md` 临时文件，一次性列出全部任务。每完成/跳过一个任务时**同步标记**。

临时文件——需要时直接覆盖旧文件，无需保留历史。模板见附录。

### 环境能力检测

首次执行 TV 前必须检测环境：

1. **支持 task list** → 预创建 10 个任务，按序执行。
2. **不支持但可切换**（如 Cursor 普通聊天 → Agent Planning）→ 告知用户并**暂停**，等用户确认切换或选择 tasks.md 方式。
3. **不支持且无法切换** → 告知用户，以 tasks.md 方式执行。

降级后：违背声明中持续标注"以 tasks.md 方式替代 task list"。

### 任务类型

Think、Verify、Decide、Output。

```
Think #1 → Verify #1 → Decide #1 → Think #2 → Verify #2 → Decide #2 → Think #3 → Verify #3 → Decide #3 → Output
```

Decide 可将后续轮次均标记为`skipped`，直接进入 Output。

### 任务规则

1. **一次性预创建全部 10 个任务**：Think #1~Decide #3 + Output。
2. **跳过剩余轮次**：仅当 Decide #N 判定**全部满足**时可跳过后续轮次进入 Output（标记为 `skipped`）。存在违背且未达上限 → 必须推进到下一轮 Think。
3. **严格顺序**：Think → Verify → Decide → (Think | Output)，不可跳过或插入。
4. **单一职责**：
   - Think：只产出一个候选方案，不验证、不决策
   - Verify：只做规则判断，不决策、不生成方案
   - Decide：唯一有权决定下一步的任务
   - 严禁将多轮压缩到单个任务中
5. **完成 ≠ 通过**：Think 产出方案即 `completed`（无论是否合规）；Verify 产出判断即 `completed`（无论满足或违背）。
6. **推进硬门控**：进入下一任务前必须先标记当前任务为 `completed` 或 `skipped`。未标记不得推进。
7. **上限 3 次**：Think → Verify → Decide 流程最多执行 3 次；未执行轮次标记 `skipped`。
8. **Output 唯一**：全流程仅 1 个 Output task，必须是最后一个。

### 任务:Think

**职责**：识别意图、整理约束、草拟**一个**候选方案。

> 🔴 **整理约束前必须先读取当前对话的 `.tv/〔conv〕/rules.yml`**。约束来源 = `.tv/〔conv〕/rules.yml` 全部规则 + 当前消息中的新要求。

❌ 不得验证规则（Verify 职责）
❌ 不得生成多个候选（多轮 Think 产生）
❌ 不得决定重试/输出（Decide 职责）

#### 深层目标理解

Think 阶段**必须**超越字面要求，独立思考用户真正目标：

- 识别用户提出该需求的**根本目的**是什么
- 发掘用户未显式提出的**隐含条件**和必要前置步骤
- 检查需求之间是否存在**潜在冲突**或互斥约束
- 若存在比字面要求**更优的达成路径**，应在候选方案中体现

> 核心原则：帮助用户更好地完成目标，而非简单地逐字执行指令。

> 深层分析的结论将驱动后续流程：识别到的冲突交给多轮 TV 迭代探索边界，发现的歧义先澄清，再把已证实的条件纳入候选方案。

> 这里的“思考”只允许做证据驱动的整理与推演；任何推导都必须建立在已证实或明确确认的条件上。

**完成**：产出一个候选方案 → `completed`。**推进**：标记后才能进入 Verify。

### 任务:Verify

**职责**：对当前轮 Think 候选方案**逐条**做规则满足/违背判断。

❌ 不得决定重试/输出（Decide 职责）
❌ 不得生成新方案（Think 职责）

**完成**：每条规则已给出判断 → `completed`（无论结果满足或违背）。**推进**：标记后才能进入 Decide。

#### 规则收集范围

> 🔴 **Verify 前置硬门控**：进入 Verify 任务前，**必须已完成** 当前对话 `.tv/〔conv〕/rules.yml` 的读取。未读取不得开始规则判断。

纳入 Verify 的规则来源（按优先级排列）：
1. **`.tv/〔conv〕/rules.yml`**（对话规则文档）— 当前对话的权威规则来源，**每轮 Verify 前重新读取**
2. 系统/平台/运行环境规则（隐式基线，始终生效）
3. 当前消息中提取的**行为规则**——仅写入符合「规则 vs 需求」判定标准的规则，任务需求不入 yml（**新增规则须同步写入 `.tv/〔conv〕/rules.yml`**）

> **规则优先级**：`.tv/〔conv〕/rules.yml` 是当前对话规则的权威来源；系统/平台规则作为隐式基线始终生效。若两者主题相同（如输出语言），以 `.tv/〔conv〕/rules.yml` 为准；系统规则中未被 `.tv/〔conv〕/rules.yml` 覆盖的条目（如安全策略、工具使用限制）仍须遵守，不得以"未写入 `.tv/〔conv〕/rules.yml`"为由忽略。

#### 规则计数

- 按可独立验证的约束项计数，与陈述形式无关
- 无法单独验证的约束合并为 1 条复合规则，比较时不得拆开

#### 验证对象

仅评估**回答正文**。违背声明本身不计入验证。

#### 规则来源标签

| 标签 | 含义 |
|------|------|
| `[系统]` | 系统提示词/平台指令/运行环境 |
| `[用户·当前]` | 当前消息中的要求 |
| `[用户·历史]` | 此前对话中的要求 |
| `[约定]` | 对话中达成的共识/偏好 |
| `[保密性规则]` | 存在但不可披露的规则 |

### 任务:Decide

**职责**：基于本轮及之前所有 Verify 结果，**决定下一步**。唯一有权调度的任务。

#### 决策规则

- **全部满足** → 跳过剩余轮次 → Output
- **违背且未达上限**（Decide #1/#2）→ 进入 Think #N+1
- **违背且达上限**（Decide #3）→ 按优先级选最优候选 → Output（列出全部违背）

#### 候选比较优先级

1. 违背条数最少
2. 并列时：可逆者优先（用户可追问恢复 > 不可恢复）
3. 仍并列：模型判断程度轻重

#### 不可逆操作

最终候选涉及**不可逆**副作用（覆盖文件、发送消息、删除数据、远程推送等）→ **不得直接执行**，必须先征得用户同意。

**完成**：已决定下一步 → `completed`。**推进**：标记后进入下一任务；若进入 Output，须先将剩余轮次标记 `skipped`。

### 任务:Output

- 全流程仅 1 个，必须是最后一个 task
- 输出最终回答标志 + 正文 + 违背声明（如有）
- **末尾必须附加统计摘要**：`> Think-Then-Verify · 第 N 轮 · 检查 M 条规则 · X 条违背`（无违背时写"无违背"）

**完成**：已输出 → `completed`。**推进**：开始前所有前序任务须已标记 `completed`/`skipped`。

## 最终输出格式

- 最终回答必须以已完成的 Verify 结果为依据；未验证、无法验证或仅凭常识/猜测补全的内容，**不得**直接输出
- Output 只负责发布结论；所有澄清与证实必须在进入 Output 前完成，进入后不得回退流程
- 最终回答中不得出现以猜测为基础的推导链，也不得使用“可能是”“或许是”等未验证表述来替代结论

```
=== 最终回答 ===

（回答正文）

> Think-Then-Verify · 第 N 轮 · 检查 M 条规则 · 无违背
```

若存在违背：

```
=== 最终回答 ===

（回答正文）

---
本回答违背了以下规则：
- [规则来源] 违背点简述
原因：为何未能完全合规

> Think-Then-Verify · 第 N 轮 · 检查 M 条规则 · X 条违背
```

## 与保密性规则的冲突处理

违背声明本身会触犯保密性规则时：

- 仍须声明违背存在，不得沉默
- 不透露保密规则具体内容
- 使用模板：`- [保密性规则] 存在一条无法在此披露的规则与用户要求冲突`

作用于：Verify 任务（标签列出存在性）、思考过程（仅披露存在事实）、违背声明（套用模板）。

## 附录：tasks.md 模板

当采用 tasks.md 单文档方式时，agent 按以下模板创建 `.tv/〔conv〕/tasks.md`。首次创建时一次性列出全部任务：

```markdown
# Tasks: Think-Then-Verify

## 第 1 轮

- [ ] Think #1：思考给出一个合理解法（简述思考方向）
- [ ] Verify #1：逐条验证（满足的勾选）
  - [ ] （规则 1 来源与内容）
  - [ ] （规则 2 来源与内容）
  - [ ] ...
- [ ] Decide #1：（勾选对应分支）
  - [ ] 全部满足 → Output
  - [ ] 部分满足 → Think #2
  - [ ] 全违背 → Think #2

## 第 2 轮

- [ ] Think #2：基于上轮违背项调整策略（简述调整方向）
- [ ] Verify #2：逐条验证（满足的勾选）
  - [ ] （规则 1 来源与内容）
  - [ ] （规则 2 来源与内容）
  - [ ] ...
- [ ] Decide #2：（勾选对应分支）
  - [ ] 全部满足 → Output
  - [ ] 部分满足 → Think #3
  - [ ] 全违背 → Think #3

## 第 3 轮

- [ ] Think #3：最后一次尝试（简述最终策略）
- [ ] Verify #3：逐条验证（满足的勾选）
  - [ ] （规则 1 来源与内容）
  - [ ] （规则 2 来源与内容）
  - [ ] ...
- [ ] Decide #3：（勾选对应分支）
  - [ ] 全部满足 → Output
  - [ ] 存在违背，选取最优 → Output
    - [ ] 候选 1
    - [ ] 候选 2
    - [ ] 候选 3

## 最终输出

- [ ] Output：输出最终回答（含违背声明，如有）
```

> 关键要求：一次性列全任务；Verify 逐条勾选；Decide 先勾分支再推进；完成后按需标记 `skipped` 或 `completed`。